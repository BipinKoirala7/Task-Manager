# MERN Task Manager

## Run it

1. Pull or clone or download the repo (if downloaded, unzip it).
2. Install everything from the root folder:

   ```sh
   npm run install-all
   ```

3. Create `backend/.env` and add your MongoDB and JWT values.
   You would need two variables `` MONGODB_URL `` & `` ACCESS_TOKEN_SECRET ``

   If you're a recruiter and want to run it instantly user this `mongodb+srv://koirala:koirala123@cluster0.6h3ehrt.mongodb.net/?appName=Cluster0` for `MONGODB_URL` && `Rj2S?RVe9[]8-dCS6A**&b5Tsg$gwbg~Bd{*QTK` for `ACCESS_TOKEN_SECRET`
5. Start the app from the root folder:

   ```sh
   npm run dev
   ```

6. Open `http://localhost:3000` in your browser.
